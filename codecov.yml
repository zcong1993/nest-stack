coverage:
  status:
    project:
      prometheus:
        flags: prometheus
      config:
        flags: config
      context:
        flags: context

flags:
  prometheus:
    paths:
      - packages/prometheus/
  config:
    paths:
      - packages/config/
  context:
    paths:
      - packages/context/
