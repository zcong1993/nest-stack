coverage:
  status:
    project:
      prometheus:
        flags: prometheus
      config:
        flags: config


flags:
  prometheus:
    paths:
      - packages/prometheus/
  config:
    paths:
      - packages/config/
